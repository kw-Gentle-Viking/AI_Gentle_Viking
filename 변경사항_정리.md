# 변경사항 정리

## AI 서버 (로컬)

---

### `poll_commands.py` — 전면 재작성

**변경 이유**: 백엔드 실제 API와 기존 코드 간 불일치 수정

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| 폴링 URL | `/commands/pending` | `/ai/commands/pending` |
| 응답 파싱 | `resp.json()` (bare list) | `resp.json().get("commands", [])` |
| ACK 로직 | 별도 ACK 호출 존재 | 제거 (백엔드가 polling 시 자동 delivered 처리) |
| ONCE callback URL | 명시적 URL 필요 | 미지정 시 `{BACKEND_WEBHOOK_URL}/ai/callback` 기본값 적용 |
| user_id 타입 | str 가정 | int → str 변환 처리 |

---

### `inference.py` — REALTIME_TICKERS 하드코딩 제거

```python
# 변경 전
REALTIME_TICKERS = ["005930", "000660"]

# 변경 후
TICKERS_FILE = "/home/user/active_tickers.json"

def load_active_tickers() -> list:
    try:
        with open(TICKERS_FILE) as f:
            return json.load(f).get("all_tickers", [])
    except Exception:
        return []
```

- `load_features()` — `REALTIME_TICKERS` → `load_active_tickers()` 동적 로드로 교체
- `active_tickers`가 비어있으면 추론 없이 조기 종료
- `main()` 로그 메시지도 동적 종목 출력으로 변경

---

### `collector_realtime.py` — REALTIME_TICKERS 하드코딩 제거 + push 추가

```python
# 변경 전
REALTIME_TICKERS = ["005930", "000660"]

# 변경 후
TICKERS_FILE = "/home/user/active_tickers.json"

def load_active_tickers() -> list: ...
```

- `main()` 시작 시 `load_active_tickers()`로 종목 로드
- `active_tickers`가 비어있으면 수집 없이 종료
- `save_1min()` — 1분봉 신규 삽입 성공 시 `push_intraday(..., "1m")` 호출
- `make_and_save_5min()` — 5분봉 신규 삽입 성공 시 `push_intraday(..., "5m")` 호출

---

### `collector_batch.py` — push 호출 추가

- `save_to_db()` — 로컬 DB 저장 성공 후 `push_intraday(..., "1m")` / `push_intraday(..., "5m")` 호출
- `save_daily_to_db()` — 로컬 DB 저장 성공 후 `push_daily(...)` 호출

---

### `push_market_data.py` — **신규 생성**

수집된 OHLCV 데이터를 백엔드로 push하는 공유 모듈.

```
POST {BACKEND_WEBHOOK_URL}/market/ohlcv
X-API-Key: {AI_SERVER_API_KEY}
{"timeframe": "1d"|"5m"|"1m", "records": [...]}
```

- push는 백그라운드 스레드에서 실행 → 수집 흐름 차단 없음
- 실패 시 로그만 남기고 조용히 넘어감 (수집기에 영향 없음)
- 환경변수: `BACKEND_WEBHOOK_URL`, `AI_SERVER_API_KEY`

---

### `backend_test_server.py` — **신규 생성** (테스트용)

백엔드 GCP 배포 전 로컬 통신 테스트용 미니 서버 (port 3000).  
백엔드의 `routes_ai_command`, `routes_ai_webhook`, `routes_market` 기능 포함.  
운영 환경에서는 사용하지 않음.

---

## 백엔드 (`/home/user/backend/`)

---

### `app/routes_market.py` — **신규 생성**

AI 서버에서 push하는 OHLCV 데이터를 수신해 `market-db`에 저장하는 엔드포인트.

```
POST /market/ohlcv
X-API-Key: {AI_SERVER_API_KEY}
```

| timeframe | 저장 테이블 |
|---|---|
| `1d` | `raw.price_daily` |
| `5m` | `raw.price_min_05` |
| `1m` | `raw.price_min_01` |

- `raw` 스키마 및 테이블 없으면 최초 요청 시 자동 생성
- 중복 데이터는 `ON CONFLICT DO UPDATE`로 덮어쓰기

---

### `app/main.py` — routes_market 라우터 등록

```python
# 추가된 내용
from app.routes_market import router as market_router
app.include_router(market_router, prefix="/market", tags=["market"])
```

---

### `backtest/db.py` — market-db 직접 쿼리로 원복

| 항목 | 변경 전 (중간 상태) | 변경 후 |
|---|---|---|
| 데이터 조회 | AI 서버 API 호출 (`GET /market/ohlcv`) | market-db 직접 SQL 쿼리 |
| `15m` 지원 | 있음 | **제거** (AI 서버가 15분봉 미수집) |
| 연결 대상 | `AI_SERVER_URL` | `MARKET_DB_URL` (기존 환경변수 유지) |

---

## 전체 데이터 흐름

```
[AI 서버 - 연구실 내부망]

  collector_batch.py / collector_realtime.py
       │ 수집
       ▼
  로컬 DB (stock_db)
       │
       │ push_market_data.py (outbound, 연구실 내부망 허용)
       ▼
[백엔드 - GCP]

  POST /market/ohlcv  →  routes_market.py  →  market-db
                                                    │
                                              backtest/db.py 조회
                                                    │
                                              백테스트 실행
```

---

## 환경변수 정리

### AI 서버

| 변수 | 설명 | 예시 |
|---|---|---|
| `BACKEND_WEBHOOK_URL` | 백엔드 GCP 서버 URL | `https://api.example.com` |
| `AI_SERVER_API_KEY` | 백엔드 인증 키 | `dev-ai-key` (운영 시 교체) |

### 백엔드

| 변수 | 설명 | 비고 |
|---|---|---|
| `AI_SERVER_API_KEY` | AI 서버 push 인증 키 검증용 | 기존 변수 그대로 사용 |
| `MARKET_DB_URL` | market-db 연결 문자열 | 기존 변수 그대로 사용 |
