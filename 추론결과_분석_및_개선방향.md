# 추론 결과 분석 및 개선 방향

> 분석 기준: 2026-05-21 ~ 2026-07-02 (정상 추론 952건, 거래정지 소급 제외)

---

## 1. 현재 추론 결과 요약

### 신호 분포

| 종목 | 매수 | 관망 | 매도 |
|------|:----:|:----:|:----:|
| 005930 삼성전자 | 375건 (78.1%) | **0건 (0%)** | 105건 (21.9%) |
| 000660 SK하이닉스 | 347건 (72.3%) | **0건 (0%)** | 133건 (27.7%) |

### 확률값 통계 (정상 추론 기준)

| 종목 | avg(매수) | avg(관망) | avg(매도) | max(관망) | std(관망) |
|------|:---------:|:---------:|:---------:|:---------:|:---------:|
| 005930 | 0.655 | 0.102 | 0.243 | 0.276 | 0.073 |
| 000660 | 0.607 | 0.097 | 0.297 | 0.263 | 0.062 |

### 시간대별 평균 매수 확률 (000660 기준)

| 시간대 | avg(매수) | avg(관망) | avg(매도) |
|--------|:---------:|:---------:|:---------:|
| 09:00 | 0.158 | 0.206 | 0.636 |
| 09:30 | 0.220 | 0.198 | 0.582 |
| 10:00 | 0.315 | 0.184 | 0.501 |
| 11:00 | 0.511 | 0.104 | 0.385 |
| 12:00 | 0.693 | 0.059 | 0.248 |
| 13:00 | 0.788 | 0.049 | 0.163 |
| 14:00 | 0.833 | 0.049 | 0.119 |
| 15:00 | 0.847 | 0.052 | 0.101 |
| 15:30 | 0.853 | 0.055 | 0.093 |

---

## 2. 핵심 문제 진단

### 문제 1 — 관망 시그널 완전 소실

관망 확률(prob_hold)의 최대값이 0.276으로, 균등 분포 기준(0.333)에도 미치지 못한다. 매수/매도 중 하나가 항상 prob_hold를 압도하기 때문에 argmax 기준으로 관망이 선택된 적이 단 한 번도 없다.

**원인 추정:**
- 학습 시 예상 클래스 분포가 매수27% / 관망45% / 매도27%이었고, 이에 맞춰 class_weight를 [1.23, 0.74, 1.23]으로 설정함
- 관망에 낮은 가중치(0.74)를 준 것이 관망 클래스 학습 자체를 약화시켰을 가능성 있음
- 또는 학습이 실제로 관망 클래스를 충분히 학습하지 못한 채 수렴한 상태 (관망이 '잘 맞히기 어려운' 클래스이기 때문)

### 문제 2 — 시간대별 단조 증가 패턴 (구조적 이상)

매수 확률이 오전 9시(0.16)부터 장 마감(0.85)까지 거의 단조증가하는 패턴이 나타난다. 이는 실제 주가 움직임과 무관하게 **시간이 지날수록 매수 확률이 높아지는** 의미이고, 모델이 종목의 실질적 방향이 아닌 시간 그 자체에 과도하게 의존하고 있다는 신호다.

**원인 추정 — `time_progress` 피처가 핵심 용의자:**
- `time_progress`는 장중 경과 시간 비율(9:00=0.0, 15:30=1.0)로, Known Future 피처로 설계됨
- TFT에서 Known Future는 encoder와 decoder 양쪽에 들어가며, 모델이 이 피처에 강한 의존을 학습하면 "오후일수록 매수"라는 편향이 생길 수 있음
- 학습 기간(2020~2023)에 한국 주식 시장이 전반적으로 상승장이었고, 장 마감 시점(15:30)에 가까울수록 당일 고점 근처에 있을 확률이 높았다면 이 편향이 학습됐을 수 있음

### 문제 3 — 분포 이동 (Distribution Shift)

학습 기간은 2020~2023년, 현재는 2026년이다. 특히 005930·000660은 반도체 사이클에 극도로 민감한 종목으로, 2024~2026년 사이 미중 반도체 규제, HBM 수요 변화 등으로 시장 환경이 크게 달라졌다. 학습 데이터에 없던 패턴이 현재 입력으로 들어오면 모델은 가장 익숙한 패턴(시간 의존성)으로 수렴한다.

### 문제 4 — 학습/추론 파이프라인 불일치 (잠재적)

`TFT_학습_전처리_가이드_수정.md`에 따르면 학습 시 클리핑과 StandardScaler 적용을 권장하고 있으나, 현재 추론 파이프라인(`inference.py`)에는 클리핑·스케일링이 전혀 없다. 학습 때 스케일링을 적용했다면 추론 시 입력 분포가 달라져 모델 출력이 왜곡된다.

---

## 3. 피처 설계 개선 방향

### 3-1. `time_progress` 처리 재검토

현재 Known Future로 분류되어 있는데, 이것이 시간대 편향의 주원인일 가능성이 높다. 두 가지 선택지:

- **옵션 A — 제거**: 모델이 시간이 아닌 가격 움직임으로만 예측하도록 강제
- **옵션 B — Unknown Past로 이동**: decoder에 노출되지 않게 해서 영향을 줄임

옵션 A가 더 근본적인 해결책이나, 시간대별 유동성 패턴(장 초반·마감 변동성)은 실제로 유용한 정보이므로 제거 전 ablation으로 확인 권장.

### 3-2. 일봉 피처의 시간적 중복 문제

현재 일봉 피처(PER, PBR, 수급 등)가 하루 종일 동일한 값으로 5분봉 전체에 broadcast된다. TFT encoder 60봉 전체에 같은 값이 반복 입력되면 attention mechanism이 이 피처에서 temporal 정보를 추출하지 못하고 static feature처럼 처리하게 된다. **일봉 피처는 Static Categorical/Numeric으로 처리하는 게 구조적으로 더 맞다.**

### 3-3. 추가 검토할 피처

| 피처 | 방향 | 이유 |
|------|------|------|
| 호가 잔량 비율 (bid/ask ratio) | 추가 검토 | 단기 방향성의 직접적 신호 |
| 체결강도 | 추가 검토 | 5분봉 단위 매수 압력 반영 |
| 전일 장후 시간외 등락 | 추가 검토 | 장 시작 방향성 힌트 |
| `phlx_semi_ret` | 005930·000660 전용 강조 | 반도체 종목의 핵심 선행 지표 |

---

## 4. 라벨 설계 개선 방향

### 4-1. threshold 재검토

현재 ±1.5% 기준으로 3등분. 5분봉 기준 1시간(12봉) 내에 ±1.5% 움직임이 얼마나 자주 발생하는지 실제 데이터로 검증이 필요하다. 너무 낮으면 관망 비율이 지나치게 줄고(노이즈 라벨), 너무 높으면 매수/매도 샘플이 부족해진다.

**추천**: 학습 기간 실제 분포를 계산해서 매수:관망:매도 ≈ 25:50:25가 되는 threshold를 역산.

### 4-2. 예측 horizon 검토

현재 12봉(1시간) 후 방향 예측. 고려할 점:
- **너무 길면**: 단기 노이즈가 커서 맞히기 어렵고, 추론 시점과 실제 매매 타이밍 사이에 슬리피지 발생
- **너무 짧으면**: 거래 비용 회수가 어려움

6봉(30분) 또는 12봉(1시간)을 유지하되, **거래 비용(수수료 ~0.015% × 2 + 슬리피지 0.05%)을 threshold에 반영**하는 것이 현실적.

### 4-3. 클래스 불균형 처리 개선

현재 CrossEntropyLoss에 고정 weight를 주는 방식인데, 실제 학습 데이터의 클래스 분포를 정확히 계산한 뒤 weight를 결정해야 한다. 추가로 **Focal Loss** 적용을 검토할 것 — 어려운 샘플(관망)에 더 집중하게 만들어 관망 소실 문제에 효과적이다.

---

## 5. 재학습 필요 여부 판단

**결론: 재학습이 맞다. 임계값 튜닝으로는 해결 불가.**

이유:
1. prob_hold 최대값이 0.276으로, softmax 이전 단계에서 이미 관망 logit이 극도로 낮음 — 출력 후처리(임계값 조정)로는 0.276짜리 관망이 선택되게 만들기 어렵고, 억지로 만들면 매우 낮은 신뢰도의 관망 신호가 됨
2. 시간대별 단조 증가 패턴은 모델 가중치 내부에 학습된 편향 — 피처 변환이나 후처리로 제거 불가
3. 2026년 현재 시장과 학습 데이터(2020~2023) 간 분포 이동은 새 데이터 추가 없이 해결 불가

**재학습 우선순위 항목:**
1. 학습 데이터에 2024~2025년 데이터 추가 (필수)
2. `time_progress` 제거 또는 Unknown Past로 이동 후 재학습, 시간대 편향 해소 확인
3. 클래스 불균형 — 실제 분포 기반 weight 재계산, Focal Loss 검토
4. 클리핑/스케일링 적용 여부 통일 (학습·추론 일치 필수)
5. 일봉 피처를 Static으로 처리하는 구조 변경 (중장기)

**재학습 없이 당장 할 수 있는 완화 조치 (임시):**
- 추론 결과에서 `prob_buy - prob_sell` 차이가 일정 미만이면 관망으로 간주하는 후처리 규칙 추가 → 자동매매 과신 방지용
- 예: `|prob_buy - prob_sell| < 0.3` 이면 시그널 무시 (혹은 관망으로 override)
