# 추론 서버 로컬 이식 가이드

로컬 환경에서 AI 추론 파이프라인 + 백엔드 서버를 함께 실행하기 위한 셋업 가이드.

---

## 1. 사전 준비물

아래 파일들을 원본 서버에서 받아야 한다.

> **DB 이식이 가장 중요하다.** 스크립트는 Git으로 받으면 되지만, DB 데이터는 직접 복사해야 한다.

| 파일/폴더 | 전달 방법 | 비고 |
|---|---|---|
| `/home/user/*.py` | Git 저장소 clone | 모든 스크립트 |
| `/home/user/best_model_state_dict.pt` | scp / USB / 구글드라이브 | **2.3MB, 반드시 직접 복사** |
| `/home/user/active_tickers.json` | Git 저장소 또는 직접 복사 | 없으면 자동 생성됨 |

> `best_model_state_dict.pt`는 Git에 올라가 있지 않으면 반드시 별도로 전달받아야 한다.

---

## 2. Miniconda 설치

```bash
# Linux
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh

# Mac (Apple Silicon)
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-MacOSX-arm64.sh
bash Miniconda3-latest-MacOSX-arm64.sh

# 설치 후 셸 재시작
source ~/.bashrc   # 또는 source ~/.zshrc
```

---

## 3. conda 환경 생성 및 패키지 설치

```bash
conda create -n kis_collector python=3.11 -y
conda activate kis_collector
```

### GPU 있는 경우 (CUDA)
```bash
pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cu121
```

### GPU 없는 경우 (CPU only)
```bash
pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
```

### 나머지 패키지
```bash
pip install \
  tft-torch==0.0.6 \
  fastapi==0.136.1 \
  uvicorn==0.47.0 \
  psycopg2-binary==2.9.11 \
  pandas==2.3.3 \
  numpy==2.4.5 \
  omegaconf==2.3.0 \
  requests==2.34.2 \
  websockets==16.0 \
  pydantic==2.13.4 \
  pandas-datareader \
  pandas_market_calendars \
  FinanceDataReader \
  OpenDartReader \
  pytorch-lightning \
  pytorch-forecasting
```

---

## 4. PostgreSQL 설치 및 DB 생성

### 설치
```bash
# Ubuntu / Debian
sudo apt install postgresql postgresql-contrib -y
sudo systemctl start postgresql
sudo systemctl enable postgresql

# Mac (Homebrew)
brew install postgresql@16
brew services start postgresql@16
```

### DB 및 유저 생성
```bash
sudo -u postgres psql <<EOF
CREATE USER stock_user WITH PASSWORD '설정할_비밀번호';
CREATE DATABASE stock_db OWNER stock_user;
GRANT ALL PRIVILEGES ON DATABASE stock_db TO stock_user;
EOF
```

### 테이블 초기화

```bash
conda activate kis_collector
cd /home/user   # 스크립트 위치

# 핵심 테이블 생성 (아래 순서대로 실행)
python init_calendar.py
python init_1day_data.py
python init_kis.py
python init_yf_fred.py
python init_dart.py
python init_intraday_features.py
python init_realtime_features.py
```

---

## 5. 환경변수 설정

`~/.bashrc` (Linux) 또는 `~/.zshrc` (Mac) 하단에 추가.

```bash
# KIS API (한국투자증권)
export KIS_APP_KEY="발급받은_KIS_앱키"
export KIS_APP_SECRET="발급받은_KIS_시크릿"

# DART API
export DART_API_KEY="발급받은_DART_키"

# PostgreSQL
export DB_HOST="localhost"
export DB_NAME="stock_db"
export DB_USER="stock_user"
export DB_PASSWORD="위에서_설정한_비밀번호"

# 백엔드 통신
export BACKEND_WEBHOOK_URL="http://localhost:3000"   # 백엔드가 같은 로컬에 있을 경우
export AI_SERVER_API_KEY="dev-ai-key"
```

```bash
source ~/.bashrc   # 적용
```

> **KIS API 키**는 한국투자증권 홈페이지에서 발급. 모의투자 계좌와 실전 계좌 키가 다르니 주의.  
> **DART API 키**는 DART 홈페이지(dart.fss.or.kr)에서 무료 발급.

---

## 6. crontab 등록

```bash
crontab -e
```

아래 내용 붙여넣기:

```cron
# 환경변수
KIS_APP_KEY=발급받은_KIS_앱키
KIS_APP_SECRET=발급받은_KIS_시크릿
DART_API_KEY=발급받은_DART_키
DB_HOST=localhost
DB_NAME=stock_db
DB_USER=stock_user
DB_PASSWORD=설정한_비밀번호
BACKEND_WEBHOOK_URL=http://localhost:3000
AI_SERVER_API_KEY=dev-ai-key

# 매일 08:00 — 거시/공시 수집
00 8 * * 1-5 /home/{사용자명}/miniconda3/envs/kis_collector/bin/python /home/{사용자명}/collector_yf_fred.py >> /home/{사용자명}/collector_yf_fred.log 2>&1
00 8 * * 1-5 /home/{사용자명}/miniconda3/envs/kis_collector/bin/python /home/{사용자명}/collector_dart.py >> /home/{사용자명}/collector_dart.log 2>&1

# 매일 08:55 — 장중 실시간 수집 시작
55 8 * * 1-5 /home/{사용자명}/miniconda3/envs/kis_collector/bin/python /home/{사용자명}/collector_realtime.py >> /home/{사용자명}/realtime.log 2>&1

# 매일 15:50 — 일별 밸류에이션/수급 수집
50 15 * * 1-5 /home/{사용자명}/miniconda3/envs/kis_collector/bin/python /home/{사용자명}/collector_kis.py >> /home/{사용자명}/collector_kis.log 2>&1

# 매일 16:00 — 장 마감 후 배치 수집
00 16 * * 1-5 /home/{사용자명}/miniconda3/envs/kis_collector/bin/python /home/{사용자명}/collector_batch.py >> /home/{사용자명}/batch.log 2>&1

# 매일 16:30 — 피처 빌드
30 16 * * 1-5 /home/{사용자명}/miniconda3/envs/kis_collector/bin/python /home/{사용자명}/build_batch_features.py >> /home/{사용자명}/build_batch_features.log 2>&1
30 16 * * 1-5 /home/{사용자명}/miniconda3/envs/kis_collector/bin/python /home/{사용자명}/build_intraday_features.py >> /home/{사용자명}/build_intraday_features.log 2>&1

# 평일 장중 5분마다 — 추론 파이프라인
*/5 * * * 1-5 /home/{사용자명}/miniconda3/envs/kis_collector/bin/python /home/{사용자명}/inference_pipeline.py >> /home/{사용자명}/pipeline.log 2>&1
```

> `{사용자명}`을 실제 사용자 이름으로 교체할 것 (예: `/home/kim/miniconda3/...`)

---

## 7. DB 이식

### DB 테이블 현황 (원본 서버 기준)

| 테이블 | 크기 | 추론 필수 | 백테스트 필요 |
|---|---|---|---|
| `realtime_features` | 509MB | ✅ | - |
| `intraday_5min` | 258MB | - | ✅ |
| `intraday_1min` | 49MB | - | - |
| `inference_features` | 13MB | ✅ | - |
| `price_daily` | 5MB | - | ✅ |
| `daily_valuation` | 3MB | - | - |
| `investor_flow_daily` | 1.6MB | - | - |
| 나머지 (calendar 등) | ~1MB | ✅ | - |

---

### 방법 1 — 전체 덤프 (권장)

전체 기능이 필요한 경우. 압축 후 약 200~300MB.

**원본 서버에서 (덤프 생성):**
```bash
pg_dump -U stock_user -h localhost -Fc stock_db > stock_db.dump
# 파일 크기 확인
ls -lh stock_db.dump
```

**파일 전송:**
```bash
# scp 사용 시 (같은 네트워크)
scp stock_db.dump {사용자}@{대상IP}:/home/{사용자명}/

# 또는 구글드라이브 / USB로 직접 전달
```

**대상 서버에서 (복원):**
```bash
# DB가 이미 생성되어 있어야 함 (4단계 완료 후)
pg_restore -U stock_user -h localhost -d stock_db -v stock_db.dump
```

---

### 방법 2 — 추론만 필요한 경우 (용량 절약)

백테스트 없이 AI 추론만 돌릴 경우. `intraday_5min`(258MB)을 제외하면 훨씬 가볍다.

**원본 서버에서:**
```bash
pg_dump -U stock_user -h localhost -Fc \
  -t realtime_features \
  -t inference_features \
  -t calendar \
  -t ticker_metadata \
  -t market_events \
  -t market_global \
  -t market_index_daily \
  -t stock_events \
  stock_db > stock_db_inference_only.dump

ls -lh stock_db_inference_only.dump
```

**대상 서버에서:**
```bash
pg_restore -U stock_user -h localhost -d stock_db -v stock_db_inference_only.dump
```

> 이 경우 `intraday_1min`, `intraday_5min`, `price_daily`는 수집기를 실행해서 신규 적재해야 한다.

---

### 복원 확인

```bash
PGPASSWORD={비밀번호} psql -U stock_user -h localhost -d stock_db -c "
SELECT relname, pg_size_pretty(pg_total_relation_size(relid))
FROM pg_catalog.pg_statio_user_tables
ORDER BY pg_total_relation_size(relid) DESC;
"
```

---

## 8. 서버 실행

### AI 추론 서버
```bash
conda activate kis_collector
cd /home/{사용자명}
uvicorn api_server:app --host 0.0.0.0 --port 8000
```

### 커맨드 폴링 (백그라운드)
```bash
nohup python poll_commands.py >> poll_commands.log 2>&1 &
```

### 백엔드 서버 (로컬 테스트용)
```bash
# 백엔드 레포 클론 후
cd /path/to/backend
uvicorn app.main:app --host 0.0.0.0 --port 3000
```

---

## 9. 동작 확인

### AI 서버 헬스체크
```bash
curl http://localhost:8000/health
# {"status": "ok", ...}
```

### START 커맨드 → 폴링 테스트
```bash
# 1. 백엔드에 커맨드 적재
curl -X POST http://localhost:3000/ai/commands/push \
  -H "Content-Type: application/json" \
  -d '{"command":"START","user_id":1,"tickers":["005930","000660"]}'

# 2. 폴링 한 번 실행
python poll_commands.py --once

# 3. active_tickers.json 업데이트 확인
cat active_tickers.json
```

### 추론 결과 push 테스트
```bash
python push_realtime_results.py
curl http://localhost:3000/ai/predictions
```

---

## 10. 파일 구조 요약

```
/home/{사용자명}/
├── best_model_state_dict.pt   ← 반드시 원본 서버에서 복사
├── active_tickers.json        ← 종목 레지스트리 (자동 생성)
│
├── api_server.py              ← AI 추론 FastAPI 서버 (port 8000)
├── poll_commands.py           ← 백엔드 커맨드 폴링
├── push_realtime_results.py   ← 추론 결과 push
├── push_market_data.py        ← OHLCV 데이터 push
│
├── inference_pipeline.py      ← 5분 추론 파이프라인 (cron 진입점)
├── inference.py               ← TFT 모델 추론
│
├── collector_batch.py         ← 16:00 배치 수집 (1분봉/5분봉/일봉)
├── collector_realtime.py      ← 08:55 장중 실시간 수집
├── collector_kis.py           ← 15:50 밸류에이션/수급 수집
├── collector_yf_fred.py       ← 08:00 거시지표 수집
├── collector_dart.py          ← 08:00 공시 수집
│
├── build_batch_features.py    ← 일봉 피처 생성
└── build_intraday_features.py ← 분봉 피처 생성
```

---

## 주의사항

- **KIS API 키는 1개 계정에서 동시에 여러 서버에서 사용 불가.** 원본 서버와 동시에 실행하면 토큰 충돌 발생.
- `best_model_state_dict.pt` 없이는 추론 불가. 반드시 먼저 전달받을 것.
- GPU 없는 환경에서는 추론 속도가 느릴 수 있으나 동작에는 문제 없음.
- Mac M1/M2는 `torch` CPU 버전으로 설치하면 됨.
