# 캡스톤 이후 백엔드 전달사항

AI 서버 ↔ 백엔드 연동 과정에서 백엔드 쪽에 반영이 필요한 사항들입니다.

---

## 1. `app/routes_market.py` — `/market/ohlcv` 404 수정

`/market/ohlcv` 엔드포인트가 404로 떨어지고 있습니다.  
Python 버전 호환성 문제일 수 있으며, 아래 수정 후 서버 재시작(또는 재배포) 부탁드립니다.

### 변경 내용

```python
# 변경 전 (Python 3.10+ 문법 — 3.9 이하에서 import 오류 발생 가능)
from typing import Literal

class OhlcvRecord(BaseModel):
    trade_date: str | None = None
    trade_datetime: str | None = None

# 변경 후 (Python 버전 무관하게 호환)
from typing import Literal, Optional

class OhlcvRecord(BaseModel):
    trade_date: Optional[str] = None
    trade_datetime: Optional[str] = None
```

### 배포 후 확인

```bash
curl -X POST http://34.64.252.181:8000/market/ohlcv \
  -H "Content-Type: application/json" \
  -H "X-Api-Key: dev-ai-key" \
  -d '{
    "timeframe": "1d",
    "records": [{
      "ticker": "005930",
      "trade_date": "2025-06-03",
      "open_price": 75000, "high_price": 76000,
      "low_price": 74500, "close_price": 75500,
      "volume": 1000000
    }]
  }'
```

정상 응답: `{"status": "ok", "timeframe": "1d", "received": 1}`

---

## 2. 거래 정지 시그널 처리 — `is_halted` 필드 대응

AI 서버에서 추론 결과를 push할 때 `is_halted` 필드가 추가됩니다.  
거래 정지 종목은 이 값이 `true`로 전송되므로, 백엔드에서 해당 필드를 보고 자동매매를 차단해야 합니다.

### AI 서버에서 전송하는 payload 구조 (`POST /ai/realtime`)

```json
{
  "inference_at": "2026-07-02T15:00:00",
  "results": [
    {
      "ticker": "005930",
      "trade_datetime": "2026-07-02T15:00:00",
      "trade_date": "2026-07-02",
      "pred_label": 0,
      "pred_str": "매수",
      "prob_buy": 0.721,
      "prob_hold": 0.183,
      "prob_sell": 0.096,
      "model_version": "best_model_state_dict.pt",
      "is_halted": false
    },
    {
      "ticker": "000660",
      "trade_datetime": "2026-07-02T15:00:00",
      "trade_date": "2026-07-02",
      "pred_label": null,
      "pred_str": "거래정지",
      "prob_buy": null,
      "prob_hold": null,
      "prob_sell": null,
      "model_version": null,
      "is_halted": true
    }
  ]
}
```

### 백엔드 처리 규칙

| `is_halted` | `pred_str` | 백엔드 동작 |
|:-----------:|:----------:|------------|
| `false` | `매수` / `관망` / `매도` | 정상 시그널 — 자동매매 로직 진행 |
| `true` | `거래정지` | **자동매매 차단** — 해당 종목 주문 일체 금지 |

### 주의사항

- `is_halted = true`인 경우 `prob_buy`, `prob_hold`, `prob_sell`, `pred_label`은 모두 `null`입니다.
- 거래 정지 판단 기준: 장중(09:00~15:30)에 해당 종목의 최신 1분봉이 15분 이상 갱신되지 않은 경우
- 정지 해제 후 정상 데이터가 들어오면 다음 추론 사이클부터 `is_halted = false`로 자동 복귀됩니다.
